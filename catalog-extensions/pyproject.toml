[project]
name = "catalog-extensions"
version = "0.1.0"
dependencies = [
  "djangorestframework",
  "django-filter",
  "drf-spectacular",
]

[tool.setuptools.packages.find]
include = ["catalog_extensions*"]
