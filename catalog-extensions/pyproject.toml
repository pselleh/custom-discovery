[project]
name = "catalog-extensions"
version = "0.2.0"
dependencies = [
  "djangorestframework",
  "django-filter",
  "drf-spectacular",
]

[tool.setuptools.packages.find]
include = ["catalog_extensions*"]
